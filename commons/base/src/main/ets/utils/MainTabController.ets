/**
 * Copyright (C), 2026-05-06
 * @author ZestBox
 * @date 2026/05/06
 * @version 1.0
 * @description: 全局 Tab 控制器
 */

import { HdsTabsController } from '@kit.UIDesignKit';

// 全局 Tab 控制器，用于控制 Tab 栏的显示/隐藏动画
export const mainTabController: HdsTabsController = new HdsTabsController();
